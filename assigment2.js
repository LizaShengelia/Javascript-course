const isIsland = true;
const population = 11;
const language = "qartuli";

if (population < 50 && !isIsland && language === "english") {
    console.log('You should live in Georgia:)')
} else {
    console.log('Georgia does not meet your criteria :(')
}
